# Optional ML dependencies for PDF_EXTRACTION_PIPELINE_ENABLED=true
# Install: pip install -r requirements-ml.txt
#
# Layout (required for pipeline — HF fallback if local weights missing):
doclayout-yolo==0.0.2
ultralytics>=8.2.85
opencv-python-headless>=4.8.0

# Formula detection + recognition (optional stages):
unimernet==0.2.1
omegaconf>=2.3.0

# OCR (optional stage):
paddlepaddle>=2.6.0; platform_system != "Darwin"
paddleocr>=2.7.3

# Table VLM (optional — requires CUDA):
struct-eqtable
