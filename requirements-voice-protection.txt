# Optional speaker + RNNoise backends for production-quality voice protection.
# Without these, MFCC fingerprint + spectral gate are used.
#
# pip install -r requirements-voice-protection.txt
#
speechbrain>=1.0.0
pyrnnoise>=0.4.0
# resemblyzer>=0.1.1
# deepfilternet>=0.5.6
